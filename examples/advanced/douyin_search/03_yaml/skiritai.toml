# Skiritai LLM 配置文件 — 抖音搜索进阶示例
# 使用 Anthropic Claude 作为 LLM
# 支持 ${VAR} 引用环境变量

[llm]
provider = "anthropic"
api_key = "${ANTHROPIC_API_KEY}"
model = "claude-sonnet-4-20250514"
temperature = 0.2
max_tokens = 4096
